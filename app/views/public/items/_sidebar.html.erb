<div class="border border-dark">
  <table class="table table-borderless">
    <tr class="border-bottom border-dark text-center"><th>ジャンル検索</th></tr>
    <tr><td><%= link_to "全ての商品",items_path, class: "text-reset" %></td></tr>
    <% genres.each do |genre| %>
    <tr><td><%= link_to genre.name, items_path(genre: genre.id) , class: "text-reset"%></td></tr><% end %>
  </table>
</div>